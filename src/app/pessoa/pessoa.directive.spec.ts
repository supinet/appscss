import { PessoaDirective } from './pessoa.directive';

describe('PessoaDirective', () => {
  it('should create an instance', () => {
    const directive = new PessoaDirective();
    expect(directive).toBeTruthy();
  });
});
